CONTEXT: You are implementing ONE task from the world-class-sites spec (Wave 0) in the
nordic-race-automation repo (xcskilabs.com — a STATIC HTML site generated by Python,
deployed via tar+ssh to SiteGround; NOT WordPress despite the wordpress/ dir name).

HARD GUARDRAILS:
- Read CLAUDE.md first — 38 numbered pitfalls; especially: #29 mu-plugins are dead
  reference code; #32 sticky CTA needs padding-bottom:80px; #33 IntersectionObserver
  guards; #34 touch targets >=44px; GA4 (G-3JQLSQLPPM) + cookie consent must be embedded
  in every page (consent-gated on xl_consent cookie) — copy the pattern from
  scripts/generate_race_pages.py, do not invent one.
- Design tokens: use the existing --gl-* palette exactly as defined inline in
  scripts/generate_race_pages.py :root (Nordic Night #1a2332 etc.). No new colors,
  no border-radius, no box-shadow.
- Fonts: Sometype Mono (data/labels), Source Serif 4 (editorial), Inter (UI) — the
  three-font system already used by the generators.
- Do NOT modify race-data/*.json. Do NOT touch scripts/deploy.py.
- Do NOT run any deploy. Generate into output/ only.
- Register: understated, brief, confident. No hype words, no exclamation marks, no
  "unlock/transform/elevate", sentence-case headings.
- Write/extend tests in tests/ (pytest) for what you build.
- Your final message must list: files changed, tests added, how to verify locally.
